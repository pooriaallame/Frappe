name: Build Frappe CRM Image

on:
  workflow_dispatch:

permissions:
  contents: read
  packages: write

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout frappe_docker
        run: |
          git clone https://github.com/frappe/frappe_docker.git frappe_docker
          cd frappe_docker
          git checkout 380b9d069ab949754fe78331af647b673984dc04

      - name: Create apps.json
        run: |
          cat > frappe_docker/apps.json <<'EOF'
          [
            {
              "url": "https://github.com/frappe/crm",
              "branch": "v1.82.0"
            }
          ]
          EOF

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v4

      - name: Login to GitHub Container Registry
        uses: docker/login-action@v4
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and Push Frappe CRM
        run: |
          docker buildx build \
            --build-arg=FRAPPE_PATH=https://github.com/frappe/frappe \
            --build-arg=FRAPPE_BRANCH=version-16 \
            --secret=id=apps_json,src=frappe_docker/apps.json \
            --tag=ghcr.io/pooriaallame/frappe-crm-isolated:1.82.0 \
            --file=frappe_docker/images/layered/Containerfile \
            --push \
            frappe_docker

      - name: Verify pushed image
        run: |
          docker pull ghcr.io/pooriaallame/frappe-crm-isolated:1.82.0

          docker run \
            --rm \
            --network none \
            --entrypoint /bin/sh \
            ghcr.io/pooriaallame/frappe-crm-isolated:1.82.0 \
            -lc '
              echo "===== APPS ====="
              ls -1 /home/frappe/frappe-bench/apps

              test -d /home/frappe/frappe-bench/apps/crm \
                && echo "CRM_APP_PRESENT" \
                || { echo "CRM_APP_MISSING"; exit 1; }

              echo "===== VERSIONS ====="
              bench version
            '
